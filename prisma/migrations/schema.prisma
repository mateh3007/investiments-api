generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
  url      = env("TEST_DATABASE_URL")
}

model investment {
  id Int @unique @default(autoincrement())
  FII String  
  initialValue Int
  numberOfShares Int
  finalValue Int 
}